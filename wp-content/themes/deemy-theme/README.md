deemy-theme (Hello Elementor Child)

Usage
- Activate `deemy-theme` in WordPress under Appearance → Themes.
- Put your custom CSS into `style.css` below the header.
- Extend `functions.php` for additional functionality as needed.

Notes
- Parent template: `hello-elementor` must be present (already found in this project).
- Handles used: `hello-elementor-parent-style` (parent), `deemy-theme-style` (child).

